name: flutter_kit
description: Debug tools for Flutter.
version: 0.0.5
homepage: https://github.com/flutter-packagist/8591_flutter_kit.git

environment:
  sdk: ">=3.5.4 <4.0.0"
  flutter: ">=3.24.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  shared_preferences: ^2.2.2
  vm_service: 14.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

